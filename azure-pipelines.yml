# Pipeline name convention
name: '$(Build.SourceBranchName)$(System.PullRequest.SourceBranch)$(Rev:.r)'

# Trigger configuration
trigger:
  - main
  - feature/*
  - release/*

pr:
  - main
  - releases/*

# Parameters
parameters:
  - name: prDeployment
    type: boolean
    default: true
  - name: branchDeployment
    type: boolean
    default: true
  - name: sndDeployment
    type: boolean
    default: true

# Resources
resources:
  repositories:
    - repository: templates
      endpoint: DEFRA
      type: github
      name: defra/fcp-pipeline-common
      # ref: pl-709  # Optional: build specific pipeline branch

# Pipeline template
extends:
  template: fcp-common-pipeline.yaml@templates
  parameters:
    appFrameworkType: nodejs   # Optional: nodejs (default), dotnet, java
    Type: kubernetes           # Optional: kubernetes (default), npm, functions, nuget
    npmLegacyPeerDeps: false   # Optional: default is false
    dockerFilePath: Dockerfile # Optional default is Dockerfile
    prDeployment: ${{ parameters.prDeployment }}
    branchDeployment: ${{ parameters.branchDeployment }}
    sndDeployment: ${{ parameters.sndDeployment }}
    # failOnThreshold: moderate
    variableGroupName: ffc-pay-batch-processor-env-var
    pr_params:
      pollingInterval: $(pr_pollingInterval)
      maxProcessingTries: $(pr_maxProcessingTries)
      disableSequenceValidation: ${{ eq(variables['pr_disableSequenceValidation'], 'true') }}
      processingActive: ${{ eq(variables['pr_processingActive'], 'true') }}
    snd2_params:
      pollingInterval: $(snd2_pollingInterval)
      maxProcessingTries: $(snd2_maxProcessingTries)
      disableSequenceValidation: ${{ eq(variables['snd2_disableSequenceValidation'], 'true') }}
      processingActive: ${{ eq(variables['snd2_processingActive'], 'true') }}
    dev_params:
      pollingInterval: $(dev_pollingInterval)
      maxProcessingTries: $(dev_maxProcessingTries)
      disableSequenceValidation: ${{ eq(variables['dev_disableSequenceValidation'], 'true') }}
      processingActive: ${{ eq(variables['dev_processingActive'], 'true') }}
    test_params:
      pollingInterval: $(test_pollingInterval)
      maxProcessingTries: $(test_maxProcessingTries)
      disableSequenceValidation: ${{ eq(variables['test_disableSequenceValidation'], 'true') }}
      processingActive: ${{ eq(variables['test_processingActive'], 'true') }}
    pre_params:
      pollingInterval: $(pre_pollingInterval)
      maxProcessingTries: $(pre_maxProcessingTries)
      disableSequenceValidation: ${{ eq(variables['pre_disableSequenceValidation'], 'true') }}
      processingActive: ${{ eq(variables['pre_processingActive'], 'true') }}
    prd_params:
      pollingInterval: $(prd_pollingInterval)
      maxProcessingTries: $(prd_maxProcessingTries)
      disableSequenceValidation: ${{ eq(variables['prd_disableSequenceValidation'], 'true') }}
      processingActive: ${{ eq(variables['prd_processingActive'], 'true') }}
