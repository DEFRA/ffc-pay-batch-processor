{{- include "ffc-helm-library.deployment" (list . "ffc-sfi-payment-batch-processor.deployment") -}}
{{- define "ffc-sfi-payment-batch-processor.deployment" -}}
spec:
  template:
    spec:
      containers:
      - {{ include "ffc-helm-library.container" (list . "ffc-sfi-payment-batch-processor.container") }}
{{- end -}}
